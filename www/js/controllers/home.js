/* global appControllers */
'use strict';

appControllers.controller('HomeCtrl',[
    '$window',
    'USER_ROLES',
    'AuthService',
    function($window, USER_ROLES, AuthService) {
        
    }
]);